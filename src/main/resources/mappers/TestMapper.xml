<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sungwon.library.app.mapper.TestMapper">
    <!-- SQL 쿼리와 매핑 구성을 작성합니다 -->
    <select id="Test">
        SELECT COUNT(*)
            FROM TBL_MEMBER
    </select>
</mapper>